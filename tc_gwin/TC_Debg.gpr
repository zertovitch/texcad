with "win32ada", "gwindows";

project TC_Debg is

   for Source_Dirs use (".", "..");
   for Object_Dir use "..\ACU_Debg";
   for Main use ("TeXCAD");

   package Builder is
      for Default_Switches ("ada") use ("-C -g -gnato -gnatwa -gnatVa -gnatecdebug.pra");
   end Builder;

end TC_Debg;

